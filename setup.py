from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Build and test different machine learning models to classify emails. The project is set up in a simple, modular way so it’s easy to clean data, train models, and check results with any email dataset.',
    author='Sultan',
    license='MIT',
)
